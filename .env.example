# Opcional. Preferível: guarde o token no cofre seguro do SO com `cmon token set`
# (cross-platform), ou deixe o cmon ler a credencial do Claude Code se logado.
#
# Este arquivo cobre o passo 1 (env var) — bom para CI ou forçar um token.
# Extrair o token atual do Claude Code no macOS:
#   security find-generic-password -s "Claude Code-credentials" -w \
#     | python3 -c "import sys,json;print(json.load(sys.stdin)['claudeAiOauth']['accessToken'])"
CLAUDE_OAUTH_TOKEN=sk-ant-oat01-...
